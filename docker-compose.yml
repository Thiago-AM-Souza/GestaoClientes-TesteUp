services:
  gestaoclientes.webapi:
    image: ${DOCKER_REGISTRY-}gestaoclienteswebapi
    build:
      context: .
      dockerfile: Backend/WebApi/GestaoClientes.WebApi/Dockerfile

  frontend:
    build:
      context: .
      dockerfile: Frontend/gestao-clientes/Dockerfile

  gestaoclientes.db:
    image: mcr.microsoft.com/mssql/server:2022-latest
